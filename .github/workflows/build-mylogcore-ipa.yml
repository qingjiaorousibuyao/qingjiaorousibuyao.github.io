name: Build MyLogCore IPA

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "MyLogCore/**"
      - ".github/workflows/build-mylogcore-ipa.yml"

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: MyLogCore

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build unsigned app
        run: |
          xcodebuild \
            -project MyLogCore.xcodeproj \
            -scheme MyLogCore \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build

      - name: Package IPA
        run: |
          mkdir -p Payload
          cp -R build/Build/Products/Release-iphoneos/MyLogCore.app Payload/
          ditto -c -k --sequesterRsrc --keepParent Payload MyLogCore-unsigned.ipa

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: MyLogCore-unsigned-ipa
          path: MyLogCore/MyLogCore-unsigned.ipa
          if-no-files-found: error
